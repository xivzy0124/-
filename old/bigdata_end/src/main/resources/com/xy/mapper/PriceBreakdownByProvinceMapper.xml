<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xy.mapper.PriceBreakdownByProvinceMapper" >
  <resultMap id="BaseResultMap" type="com.xy.pojo.PriceBreakdownByProvince" >
    <result column="provincename" property="provincename" jdbcType="VARCHAR" />
    <result column="varietyname" property="varietyname" jdbcType="VARCHAR" />
    <result column="min_price" property="minPrice" jdbcType="DECIMAL" />
    <result column="max_price" property="maxPrice" jdbcType="DECIMAL" />
    <result column="avg_price" property="avgPrice" jdbcType="DECIMAL" />
    <result column="tradingvolume" property="tradingvolume" jdbcType="DECIMAL" />
    <result column="marketname" property="marketname" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="insert" parameterType="com.xy.pojo.PriceBreakdownByProvince" >
    insert into Price_breakdown_by_province (provincename, varietyname, min_price, 
      max_price, avg_price, tradingvolume, 
      marketname)
    values (#{provincename,jdbcType=VARCHAR}, #{varietyname,jdbcType=VARCHAR}, #{minPrice,jdbcType=DECIMAL}, 
      #{maxPrice,jdbcType=DECIMAL}, #{avgPrice,jdbcType=DECIMAL}, #{tradingvolume,jdbcType=DECIMAL}, 
      #{marketname,jdbcType=VARCHAR})
  </insert>
  <select id="selectAll" resultMap="BaseResultMap" >
    select provincename, varietyname, min_price, max_price, avg_price, tradingvolume, 
    marketname
    from Price_breakdown_by_province
  </select>

  <select id="selectByName" resultMap="BaseResultMap" >
    select provincename, varietyname, min_price, max_price, avg_price, tradingvolume,
           marketname
    from Price_breakdown_by_province
    <where>
      provincename not like '%自治区%' and provincename not like '%市%'
      <if test="name!=null">
        and varietyname = #{name}
      </if>
    </where>
  </select>

  <select id="calculateTotalValue" resultType="java.math.BigDecimal">
    SELECT sum(avg_price*tradingvolume) as totalValue
    FROM price_breakdown_by_province
    WHERE varietyname = #{name}
    GROUP BY varietyname
  </select>

  <select id="calculateTotalVolume" resultType="java.math.BigDecimal">
    SELECT sum(tradingvolume) as totalVolume
    FROM price_breakdown_by_province
    WHERE varietyname = #{name}
    GROUP BY varietyname
  </select>

  <select id="countMarkets" resultType="java.lang.Integer">
    SELECT count(marketname) as marketCount
    FROM price_breakdown_by_province
    WHERE varietyname = #{name}
    GROUP BY varietyname
  </select>


</mapper>